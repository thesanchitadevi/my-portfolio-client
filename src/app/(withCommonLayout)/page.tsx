const HomePage = async () => {
  // Data fetching
  // const res = await fetch("http://localhost:5000/products")

  return (
    <div className="p-4 text-center">
      <h1 className="text-3xl font-bold py-5">Welcome to Home</h1>
    </div>
  );
};

export default HomePage;
